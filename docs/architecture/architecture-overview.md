# Architecture Overview

GenesisForgeAI is a repository template and orchestration layer with:
- Instruction system (`/instructions`)
- Template catalog (`/templates`)
- Bootstrap/init workflow (`/scripts/init-project.sh`)
- VS Code extension (`/vscode-extension`)
- Documentation engine (`/docs`)
