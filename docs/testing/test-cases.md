# Test Cases

1. Run init for React+TS and verify `project.json` fields.
2. Run init twice and verify no duplicate folders.
3. Create task folder and verify required files.
4. Trigger documentation generation command and verify updated timestamp.
